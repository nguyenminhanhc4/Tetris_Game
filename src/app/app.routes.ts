import { Routes } from '@angular/router';
import { GameContainerComponent } from './features/game/components/game-container/game-container.component';

export const routes: Routes = [
  { path: '', component: GameContainerComponent },
];
